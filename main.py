import generator
import csv

"""Main file
    1. read inputs
        load the information to the database 
"""



def read_and_load(file_path):
    # while loop that takes user input and save it to a file 
    # prompt when user enter exit then it exits
    while(True):
        

def init_jet(file_path):
    # read and load call 
    # read the csv file and load to the global dict